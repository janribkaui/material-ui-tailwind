/**
 * Merges multiple class names together.
 *
 * @param {ClassValue[]} inputs - The class names to merge.
 * @returns {string} The merged class names.
 */
declare const mergeStyles: () => string;
export default mergeStyles;
